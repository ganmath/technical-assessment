<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="statisticsPersistenceUnit" transaction-type="JTA">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- JTA DataSource -->
        <jta-data-source>java:comp/env/jdbc/AssessmentDataSource</jta-data-source>

        <!-- Entities -->
        <class>com.assessment.model.VectorEntity</class>
        <!-- Add other entity classes if needed -->

        <!-- Properties -->
        <properties>
            <!-- Specify your database dialect -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <!-- Specify the JDBC driver -->
            <property name="hibernate.connection.driver_class" value="org.postgresql.Driver"/>
            <!-- Specify the JDBC URL -->
            <property name="hibernate.connection.url" value="jdbc:postgresql://localhost:5432/production_db"/>
            <!-- Specify the database username -->
            <property name="hibernate.connection.username" value="postgres"/>
            <!-- Specify the database password -->
            <property name="hibernate.connection.password" value="omkar"/>
            <!-- Set to create-drop to re-create the database on each deployment -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <!-- Specify additional Hibernate properties if needed -->

            <!-- Example: Configuring Hibernate to show SQL queries in the console -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>